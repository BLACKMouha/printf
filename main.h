#ifndef MAIN_H
#define MAIN_H

/** ===== C Header Files ===== */
#include <stdarg.h>
#include <stdlib.h>
#include <stdio.h>
#include <limits.h>
#include <unistd.h>

/** ===== Function Prototypes ===== */
int _printf(const char *format, ...);
int _putchar(char c);
int _strlen(const char *s);
int _strcmp(const char *s1, const char *s2);
int _puts(char *s);

#endif /* MAIN_H **/
